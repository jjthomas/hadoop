package org.apache.hadoop.hdfs.inotify;

import org.apache.hadoop.hdfs.server.namenode.FSEditLogOp;

public class FSEditLogOpTranslator {

  public static Event translate(FSEditLogOp op) {
    if (op instanceof )
  }
}
